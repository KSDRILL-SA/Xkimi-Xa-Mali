# Session record — 2026-09-05

Four external audits received, verified and documented. Their surviving work
consolidated into a plan and then built: **Phases 1 and 2 complete**, eighteen
work items across thirteen merged pull requests (#490–#502).

---

## Part 1 — the audits (#485–#488)

Four rounds, 58 numbered findings. Each was checked against the code **before**
it entered the backlog, and that verification changed the work materially.

| | Findings | Stood | Notable |
|---|---|---|---|
| A1 — master audit | 13 | 12 | F12 already fixed |
| A2 — state machines, concurrency, security | 20 | 17 | 3 endorsements, 1 answered by existing code |
| A3 — money flow, API integrity | 12 | 12 | 5 rediscoveries |
| A4 — API, authorisation, attack | 13 | 2 | **11 hypotheses did not hold** |

### Why verification came first

**A1-F12** is filed P1 and was already fixed: `VERCEL_ENV=production` is first
and unconditional in `isLiveDeployment`, pinned by a test across every declared
value. Implementing the recommendation would have moved correct code back toward
the shape that caused the phantom payment.

The cause was traced in L8: the deployment docs still described the old
resolution order. **Stale documentation cost a whole finding.**

### The fact that reordered everything

There is no payment gateway. The DebiCheck application was declined; a live
deployment selects `disabledGateway`. So the audits' P0s — the Netcash lifecycle
— are real and **dormant**, while a two-admin race could lock leadership out of
the console that afternoon. The plan is ordered by what can produce a wrong
outcome now, not by severity.

**Six of the auditors' ten top blockers require a gateway that does not exist.**

---

## Part 2 — Phase 1, what was live (#490–#497)

### The one that mattered most

**L1.** Two admins removing each other concurrently could leave zero admins —
each operation individually legitimate and individually permitted. Worse, two
*different* operations break the same invariant and could not see each other:
revoking a role reads `user_roles`, suspending an account reads `users`.

There is no check that fixes this: under READ COMMITTED each transaction sees its
own delete and not the other's. A transaction-scoped advisory lock, **named for
the invariant rather than either operation**, is what closes it.

**And one the audits missed entirely.** The member app's `setMemberStatus`, at
`POST /api/v1/admin/members/:id/status`, had *no last-admin guard at all* — not a
race, a single request. `status-policy` exists precisely because suspension can
strand the circle the way role revocation can, and the console's copy was given
the rule. This one, one file over, was never given it.

> Sharing a rule only helps the callers that ask it.

### What writing a test found

**L9** made the money-handling contract enforceable instead of commented — and
found four real bugs doing it:

| File | Expression |
|---|---|
| `budget.service` | `mandateAmount - budgetAmount` |
| `report.service` | `totalDue - totalPaid` |
| `insights.service` | `ytd + monthly * remainingMonths` |
| `goal-engagement` | `Math.round(amount * 100) / 100` |

The last is the interesting one: a correct-*looking* copy of `roundZAR` that
rounds **differently** at a `.xx5` boundary. And the third exposed a gap in the
contract itself — it banned `x * n` and offered nothing to write instead, so the
projection routed around it. `multiplyZAR` now exists.

### Two decisions taken on the owner's behalf

Delegated explicitly while the work continued overnight. Both argued from what
the system does today, both reversible, **both should be reviewed**.

- **D1 — the pool may not go negative.** Every debit undoes a specific credit;
  there is no disbursement. `reconcileLedger` was already relying on this in one
  query and saying it nowhere else. What would change it: payouts.
- **D2 — an overpayment stands, and somebody is told.** Rejecting it would make
  the system disagree with the bank about money that physically arrived.
  Crediting forward or refunding automatically moves a member's money with nobody
  deciding to.

### And one correction to our own record

**A2-F30 withdrawn.** We had written that `TRUSTED_PROXY` should be confirmed set
in production "or client IPs are silently absent everywhere". Wrong:
`resolveTrustedProxy` falls back to `vercel` for any absent value, and a test
pins it. That removed an owner action that should never have been on the list.

---

## Part 3 — Phase 2, the patterns (#498–#502)

Done **before** the gateway work, not after. Every item is a guarantee that
existed on one path and not its sibling — the audits' own thesis. Building a
settlement pipeline on paths that still disagree bakes the disagreement in.

### The largest: a state for "we do not know"

**P6.** The system could say `SUCCESS` or `FAILED` about a money movement and
nothing else. So when a submission timed out — request gone, no answer, the bank
possibly holding the member's money — it had to guess.

**Two schedulers guessed differently, and that is the tell:**

- the debit run called it `FAILED`, and `transaction-retry-failed` collects
  exactly `FAILED` — so a timeout on a debit the bank had accepted was
  **submitted again**;
- the goal plan called anything not `FAILED` collected, and reset `failedRuns` on
  a `PENDING` that had settled nothing — so a plan could submit forever and
  **never pause**.

`UNKNOWN` is deliberately not an inflow: money whose fate nobody knows has not
been received. Nobody resolves one automatically yet, and that is honest rather
than unfinished — the thing that will is the load report, which is Phase 3.

### The claim that was not a claim

**P2.** The comment said *"check what already exists, claim, then submit."* There
was no claim: it read, called the gateway, and inserted afterwards. Two
concurrent requests both read nothing, both submitted, and collided **after** the
member had been charged twice.

Second time this repository has paid for that lesson. **A comment asserting a
safety property is not evidence of one.**

### The one that ran the other way

**P8.** The audit said a resumed plan could produce two `ACTIVE` plans. It cannot
— the partial unique index refuses it. Everywhere else in this work the
application held a rule the database did not; here the database held one the
application had forgotten, and quietly absorbed a case nobody had thought about.
The defect was only that the member got a raw constraint error.

---

## What is not done, and why

**Phase 3 (the Netcash lifecycle, 8 items) is planned and unscheduled.** It needs
a gateway that does not exist, and enabling one is a business decision — the
DebiCheck application was declined, and the alternatives (another provider, an
aggregator, NASASA) are the owner's call.

One recommendation carried forward: **make load-report polling the primary
settlement mechanism and the webhook an optional accelerant.** The polling
contract is documented and buildable today; the postback contract is not
established, and the current endpoint would reject a genuine Netcash post at the
signature check.

**Phase 4 (certification)** needs credentials.

**D3** — is "last day of the month" a first-class debit day? — blocks G8 and only
G8. It can wait for the gateway decision.

---

## Open items for the owner

| Item | Note |
|---|---|
| **Review D1 and D2** | Two money decisions taken by the implementer. Both reversible; the reasoning is in `implementation-plan.md` |
| **The gateway decision** | Nothing in Phase 3 should start until this is answered |
| Private key in a second password manager | From 2026-09-04. Still one copy, still a single point of failure for every backup |
| Docker / PG 18 client | The emergency restore path only |
| Broadcast retry | Still unconfirmed |
| BulkSMS billing, Netcash / NASASA | Unchanged |

## Repo state at #502

All three apps deployed. **1,605 tests green** in web across 121 files, 387 in
admin, 179 in utils, 100 in database; typecheck 3/3; lint 0 errors.

Two migrations added: `20260905080000_unknown_outcome`,
`20260905090000_mandate_gateway_sync`.

---

# The rest of the day — #504 to #510

## Audit 5: the provider's own contract, read against the code

Not an external reviewer. **Netcash's Appendix A**, read line by line against
the adapter, and it found the most serious defect of all sixty-three: every
collection submitted `contribution + fee` while the mandate registered the bare
contribution as **both** the Instalment and the Maximum — so §10.6.3 made every
single collection qualify as a dispute, against a 0.5% threshold that is a
quarter of one collection at fifty members.

Also fixed: four different collection references where the contract requires one
per payer that never changes (§3.3/§18.9); four presentments against a limit of
two (§16.1); and a bank-suspended mandate that told nobody (§15.2.1).

**Nothing in audits 1 to 4 found any of them**, because finding them required
reading the document we would sign rather than the code we had written. The
lesson is recorded beside the sandbox requirement in `implementation-plan.md`:
**read the service terms before writing the adapter.**

## The documentation caught up with the system (#507)

The Founder Guide still told a joining member to "set up your debit order".
Fifteen passages described a collection mechanism that does not exist. Rewritten
as the third edition around what actually happens — the member pays, an
administrator records it with proof.

The compliance brief had been written against the wrong applicant: **KSDRILL SA
(Pty) Ltd** is the applicant, an existing registered company, not a new stokvel.
Rewritten around that, since it is the strongest asset the application has.

Constitution clause 6.1 names the Capitec account; members pay into ABSA. The
constitution was signed 2026-08-24, so the markdown cannot fix it — the
discrepancy is disclosed under the clause and the amendment is drafted ready to
sign in `resolution-2026-09-banking.md`.

The four `NEXT_PUBLIC_GROUP_*` variables holding the banking details members are
shown existed in exactly one file: the code that reads them. Now documented in
three places and reported by the go-live preflight.

Widening the doc-drift guard over the collections documents immediately found
three more stale claims, including a due-diligence pack telling a bank there
were 16 migrations when there were 51, and listing backups and disaster recovery
as open gaps when both were closed and restore-proven.

## Two features that could not work (#508)

Both from the same root: built assuming a debit order would honour them.

**Monthly goal plans were unreachable.** Enrolment demanded a mandate and the
daily run paused any plan without one — so with no provider, the app offered the
feature and refused every attempt. A plan is a member's standing commitment; a
debit order was one way of honouring it. Now, when nothing can collect, the
monthly event is a **request**, and an administrator recording an offline
payment against that goal closes the month. `lastRequestedPeriod` is a new
column rather than a reuse of `lastCollectedPeriod`, because a plan that was
merely asked must never look collected.

**An overpayment did nothing.** It sat on one month, alerted, and the surplus
was forgotten — the next month opened owing the full amount and the member who
believed they had paid twice was chased for it. Payments are now split across
the months they cover: the named month first, then unsettled months oldest
first. It never invents a period and never touches a waived one, and the pool is
credited every part rather than only the first.

## A monitoring line that cried wolf (#509)

The production build log printed **"deployment is LIVE; payment gateway is
mock"**. Never true — `selectGateway` returns the disabled adapter there. The
line was reading `PAYMENT_GATEWAY`, an intention, and calling it the gateway.

That sentence is a word-for-word description of the phantom-payments incident
this project actually had. An alarm that cries wolf about the exact failure it
exists to catch is worse than no alarm. It now reports the adapter that gets
selected, pinned by a property test over all eight combinations.

## The guide stopped saying we were refused (#510)

Owner's instruction. The member guide now says collections are **being
arranged**, and that a partner needs to see a proven record of contributions
already coming in — which is true, and doubles as the reason the monthly proof
of payment matters. The decline and the reapplication argument stay in
`docs/compliance/`, which is written for banks rather than members.

## Repo state at close

`main` at #510. All three apps deployed and serving. Migrations applied
automatically on the production build; the go-live preflight reports clean.

Three migrations added across the day: `20260905080000_unknown_outcome`,
`20260905090000_mandate_gateway_sync`, `20260905200000_goal_plan_offline`.

**Development pauses here.** See `docs/stage-close-2026-09-05.md`.
