{
  "name": "@xxm/observability",
  "version": "0.0.1",
  "private": true,
  "description": "Shared structured logging and error reporting for the authenticated apps",
  "exports": {
    ".": "./src/index.ts",
    "./logger": "./src/logger.ts"
  },
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "sideEffects": false,
  "peerDependencies": {
    "@sentry/nextjs": ">=10.0.0"
  },
  "scripts": {
    "test": "vitest run --config vitest.config.ts"
  },
  "devDependencies": {
    "@sentry/nextjs": "^10.67.0",
    "vitest": "^4.1.10"
  }
}
